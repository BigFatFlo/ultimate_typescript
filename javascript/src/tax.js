// We can disable type checking for this file by adding // @ts-nocheck at the top of the file
// We can use JSDoc to add type information to the function
/**
 * Calculates income tax
 * @param {number} income - net salary after expenses
 * @returns {number}
 */
export function calculateTax(income) {
    return income * 0.3;
}

export function calculateTax2(income) {
    return income * 0.3;
}
